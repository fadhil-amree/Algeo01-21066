import java.util.*;
package src;
import src.Matrix;
import Determinan.*;

public class Determinan {

    public static void main (int menu)
    {
        // KAMUS
        int i, j, n;

        // ALGORITMA

        if (menu == 1)
        {

            // Baca Matrix
            float[][] matrix;

            // Output Matrix

            // Output Determinan
        } else // menu == 2
        {
            // Baca Matrix
            float[][] matrix;

            // Output Matrix

            // Output Determinan
            System.out.println();
            System.out.println(detKofaktor(matrix));
        }
    }
}